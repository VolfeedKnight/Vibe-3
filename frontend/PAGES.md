# GitHub Pages

This frontend is published by the `Deploy frontend to GitHub Pages` workflow.

For demo backend access, run a Cloudflare Quick Tunnel locally and pass the
generated `trycloudflare.com` URL to the workflow's `api_base_url` input, or use
the backend connection card in the deployed app.
